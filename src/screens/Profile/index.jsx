import React from 'react';

import { Container, Text } from '../../shared/styles';

const Profile = () => {
	return (
		<Container>
			<Text>Profile</Text>
		</Container>
	);
};

export default Profile;
